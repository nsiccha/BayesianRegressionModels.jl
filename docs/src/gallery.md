# Gallery

BRM ships a gallery of presets and worked examples. Each card shows
the input formula, the SLIC submodel body emitted by sbimpl, the
auto-generated posterior-predictive check, and the transpiled Stan
source. Click a label to open the full pipeline view.

```@raw html
<div class="htmxo-embed" data-hx-base="live-brm/pipeline/gallery" hx-swap="innerHTML">
  <em>Loading BRM gallery…</em>
</div>
```


