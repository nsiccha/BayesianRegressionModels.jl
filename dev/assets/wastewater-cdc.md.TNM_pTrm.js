import{_ as o,o as t,c as a,a5 as n}from"./chunks/framework.BBrGrNbV.js";const u=JSON.parse('{"title":"The full CDC ww-inference-model","description":"","frontmatter":{},"headers":[],"relativePath":"wastewater-cdc.md","filePath":"wastewater-cdc.md","lastUpdated":null}'),i={name:"wastewater-cdc.md"};function r(s,e,c,d,l,h){return t(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="The-full-CDC-ww-inference-model" tabindex="-1">The full CDC <code>ww-inference-model</code> <a class="header-anchor" href="#The-full-CDC-ww-inference-model" aria-label="Permalink to &quot;The full CDC `ww-inference-model` {#The-full-CDC-ww-inference-model}&quot;">​</a></h1><p>This page ports the complete CDC <a href="https://github.com/CDCgov/ww-inference-model" target="_blank" rel="noreferrer"><code>ww-inference-model</code></a> — a <strong>joint wastewater + hospital-admissions</strong> renewal model — onto BRM&#39;s StanBlocks backend. Unlike the <a href="/BayesianRegressionModels.jl/dev/wastewater">reduced single-catchment example</a>, this is the full model from the upstream <code>model_definition.md</code>, and it is verified end to end (transpile + <code>stanc</code> + finite BridgeStan density/gradient, dim ≈ 380) by <code>test/cdc_ww_inference.jl</code>.</p><h2 id="Why-this-one-is-NOT-the-four-pane-view" tabindex="-1">Why this one is NOT the four-pane view <a class="header-anchor" href="#Why-this-one-is-NOT-the-four-pane-view" aria-label="Permalink to &quot;Why this one is NOT the four-pane view {#Why-this-one-is-NOT-the-four-pane-view}&quot;">​</a></h2><p>Every other model page uses the four-pane split — <em><code>@brm</code> authoring → StanBlocks → Stan → Turing</em>. That split is, by definition, a comparison of a <strong><code>@brm</code> formula model</strong> across backends. This model is not a <code>@brm</code> formula model, and cannot be: its structure exceeds the formula DSL. Specifically —</p><ul><li><p>it has a <strong>multi-level</strong> shape: independent per-subpopulation renewal cells whose outputs are <strong>aggregated</strong> (population-weighted) and then feed a <strong>second</strong> observation stream (hospitalizations) at the jurisdiction level; and</p></li><li><p>it carries several <strong>latent parameter processes</strong> — a differenced-AR weekly log-Rᵘ, per-subpopulation AR(1) deviations, an AR(1)-logit IHR, a day-of-week Dirichlet — that are not formula terms.</p></li></ul><p>So it lives at BRM&#39;s <strong>backend layer</strong> (<code>@slic</code> + <code>@deffun</code> over StanBlocks), which <em>can</em> host it. The honest rendering below is therefore the two panes that apply: the exact model source, and the Stan it emits (whose <code>functions {}</code> block contains the <code>@deffun</code> scan recipes). This is the concrete answer to &quot;how well can BRM support these models&quot;: the backend hosts the full model; the formula surface does not reach it.</p><h2 id="The-model" tabindex="-1">The model <a class="header-anchor" href="#The-model" aria-label="Permalink to &quot;The model {#The-model}&quot;">​</a></h2><p>Four coupled pieces, faithful to <code>model_definition.md</code>: 2. <strong>Infection process.</strong> Reference-subpopulation weekly unadjusted log-Rᵘ follows a differenced autoregression; the realized reproduction number carries <strong>infection feedback</strong> <code>R(t)=Rᵘ(t)·exp(−γ Σ_τ I(t−τ)g(τ))</code>; incidence follows the renewal equation with a 50-day exponential seeding phase. (<code>dar_logru</code>, <code>renewal_feedback</code>.)</p><ol start="3"><li><p><strong>Multi-subpopulation hierarchy.</strong> K catchments (+ a reference) each carry <code>log Rᵘ_k = log Rᵘ_0 + m + δ_k(t)</code> with δ_k an AR(1) deviation (<code>ar1_dev</code>); each runs its own renewal, and jurisdiction incidence is the population-weighted aggregate<code>I(t)=Σ_k w_k I_k(t)</code>.</p></li><li><p><strong>Wastewater observation (per subpopulation).</strong> Genome concentration is the shedding-kinetics convolution of that subpopulation&#39;s infections scaled by genomes-per-infection / per-person volume (<code>shed_convolve</code>); site-lab measurements are log-normal with a site scaling and a hierarchical observation SD, <strong>left-censored below the limit of detection</strong>.</p></li><li><p><strong>Hospital admissions (jurisdiction).</strong> Expected admissions convolve aggregate infections with an infection-to-hospitalization delay (<code>delay_convolve</code>), scaled by a time-varying AR(1)-logit IHR and a day-of-week Dirichlet effect; <strong>negative-binomial</strong> counts.</p></li></ol><p>The carried-state parts (renewal + feedback, the AR processes, all convolutions) are <code>@deffun</code> Stan functions — <code>plate</code> broadcasts the per-subpopulation renewal + wastewater cell over <strong>independent</strong> subpopulations, and the aggregate + hospitalizations are ordinary top-level statements on the collected infection matrix.</p><p>The hidden setup below evaluates the <code>@deffun</code> helpers and the fixture from the checked-in source before the model is rendered.</p><p>The declaration below is extracted verbatim from <code>cdc_ww_inference_model</code> in the reviewed source, followed by the Stan it emits (the <code>@deffun</code> renewal / AR / convolution scans appear in the generated <code>functions {}</code> block).</p><h2 id="Provenance" tabindex="-1">Provenance <a class="header-anchor" href="#Provenance" aria-label="Permalink to &quot;Provenance {#Provenance}&quot;">​</a></h2><p>Reproduction: <a href="https://github.com/nsiccha/BayesianRegressionModels.jl/blob/main/research/wastewater/cdc_ww_inference.jl" target="_blank" rel="noreferrer"><code>research/wastewater/cdc_ww_inference.jl</code></a>, gated by <code>test/cdc_ww_inference.jl</code>. The generation-interval, shedding-kinetics and delay PMFs and the priors are illustrative discretizations, not calibrated values; &quot;faithful&quot; refers to the CDC model&#39;s structure. Model artifact only — it is not sampled here.</p>',14)])])}const f=o(i,[["render",r]]);export{u as __pageData,f as default};
